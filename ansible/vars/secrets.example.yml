---
# Copy this file to vars/secrets.yml and fill in real values.
# Do NOT commit vars/secrets.yml.

# Generate a random 64-hex string (32 bytes) for encryption key
# Example:
#   openssl rand -hex 32
n8n_encryption_key: "REPLACE_WITH_64_HEX_CHARS"

# Basic Auth
n8n_basic_auth_user: "telemaco"
n8n_basic_auth_password: "REPLACE_WITH_STRONG_PASSWORD"

# Postgres
postgres_password: "REPLACE_WITH_STRONG_PASSWORD"
